# Ops Analytics Lab

Files in this lab:

- `services.csv`: service ownership and SLO targets
- `deploys.csv`: recent deploy history
- `jobs.jsonl`: job execution logs with failures and durations
- `incidents.db`: SQLite database created from `incidents.sql`
- `handoff.md`: on-call notes from the previous shift

Use `/home/agent/work` for reusable reports, filtered datasets, or scratch files.
