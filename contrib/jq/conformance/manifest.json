{
  "suites": {
    "jq": {
      "entries": {
        "conformance/jq.test.sh::default output preserves object key order": {
          "mode": "xfail",
          "reason": "gbash jq marshals objects with sorted keys instead of preserving jq's default key order"
        }
      }
    }
  }
}
